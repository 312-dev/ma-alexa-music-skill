pytest>=8

# The aiohttp adapter (ma_provider/webserver.py) is Ampere's public front door
# when it runs inside Music Assistant. MA brings aiohttp itself, but the tests
# have to be able to exercise that adapter on a laptop that has never heard of
# Music Assistant, which is the whole point of the framework-free core.
aiohttp>=3.9
pytest-aiohttp>=1.0

# The config-entry model, so the setup rail (ma_provider/wizard.py) is tested
# rather than skipped. Without it the code that decides whether an operator can
# finish setup at all is the least covered code in the project, which is how a
# form that hides the button you need ships unnoticed.
#
# Only the models package. The Music Assistant server itself is not a
# reasonable test dependency, so the handful of tests that genuinely need it
# still skip.
music-assistant-models>=1.1.129
